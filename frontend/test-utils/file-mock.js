/**
 * Mock file so that React components which import SCSS or SVG files (via webpack)
 * can still be tested.
 * Configured for Jest in package.json ("moduleNameMapper" setting)
 */

module.exports = 'file content';
