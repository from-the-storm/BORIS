doctype html
html
    head
        title BORIS: Invalid Login Token

        link(href="https://fonts.googleapis.com/css?family=Kanit:700|Noto+Sans", rel="stylesheet")

        style.
            body {
                font-family: 'Noto Sans', sans-serif;
            }
            h1, h2, h3, h4, h5 {
                font-family: 'Kanit', sans-serif;
                font-weight: bold;
                text-transform: uppercase;
            }

body
    h1 Invalid Login Link
    p.error-message.
        The login link that you used was invalid. Each link expires after one week,
        so if it's been more than a week, try requesting a new login link.
